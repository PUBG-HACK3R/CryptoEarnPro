import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/contexts/AuthContext";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "CryptoEarn Pro - Professional Crypto Investment Platform",
  description: "Secure cryptocurrency investment platform with guaranteed daily returns. Invest in BTC, ETH, USDT and earn consistent profits.",
  keywords: "cryptocurrency, investment, bitcoin, ethereum, crypto earning, passive income",
  authors: [{ name: "CryptoEarn Pro" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} antialiased`}
      >
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
